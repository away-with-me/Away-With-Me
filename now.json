{
  "version": 2,
  "name": "away-with-me",
  "alias": ["away-with-me.now.sh"],
  "builds": [
    {
      "src": "package.json",
      "use": "@now/static-build",
      "config": { "distDir": "dist" }
    }
  ]
}
